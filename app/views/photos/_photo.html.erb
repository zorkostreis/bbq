<%= link_to (image_tag photo.photo.variant(resize_to_fill: [200, 200])),
  rails_blob_url(photo.photo),
  data: { lightbox: 'photo-set', title:
    "#{link_to(photo.user.name, user_path(photo.user))}
     #{link_to(
        bi_icon('trash-fill'),
        event_photo_path(photo.event, photo),
        method: :delete,
        title: t('buttons.delete_photo'),
        data: {confirm: t('photos.destroy_confirm')}
    ) if current_user_can_edit?(photo)}"
  }
%>
