<div class='event-photo mt-2 mb-2'>
  <%= link_to image_tag(photo.photo.variant(resize_to_fit: [150, 150])), url_for(photo.photo) %>

  <%= t('photos.added') %> <%= render partial: 'users/user', object: photo.user %>
</div>
