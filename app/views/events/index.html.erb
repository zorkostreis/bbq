<h1><%= I18n.t('headings.events') %></h1>

<div class='my-4'>
  <%= link_to t('buttons.event.create'), new_event_path, class: 'btn btn-success' %>
</div>

<div class='row mb-2'>
  <%= render partial: 'events/event', collection: @events %>
</div>
