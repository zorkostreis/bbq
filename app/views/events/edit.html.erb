<h1>Редактирование события</h1>

<div class='row'>
  <div class='col-md-6 mt-2'>
    <%= render 'form', event: @event %>
    
    <%= link_to 'Удалить событие', event_path(@event), method: :delete,
      data: { confirm: t('are_you_sure') }, class: 'mt-3 btn btn-danger' %>
  </div>
</div>
