<h1><%= I18n.t('headings.event_editing') %></h1>

<div class='row'>
  <div class='col-md-6 mt-2'>
    <%= render 'form', event: @event %>
    
    <%= link_to t('buttons.event.delete'), event_path(@event), method: :delete,
      data: { confirm: t('are_you_sure') }, class: 'mt-1 btn btn-danger' %>
  </div>
</div>
