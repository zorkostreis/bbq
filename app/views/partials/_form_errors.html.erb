<% errors = model.errors %>

<% if errors.any? %>
  <div id="error_explanation" class="alert alert-danger">
    <h5><%= pluralize(errors.count, "error") %> prohibited this user from being saved:</h2>

    <ul>
      <% errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
